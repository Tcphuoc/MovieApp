import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign up - Movie App",
  description: "Generated by create next app",
};

export default function SignUpLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return <>{children}</>;
}
